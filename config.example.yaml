server:
  port: 8080
  mode: dev        # dev | prod
  node_id: node-1  # unique per instance

redis:
  addr: "127.0.0.1:6379"
  password: ""
  db: 0

mysql:
  dsn: "root:<password>@tcp(localhost:3306)/im_system?charset=utf8mb4&parseTime=True"

jwt:
  secret: "<your-secret-key>"

kafka:
  brokers:
    - "localhost:9092"
